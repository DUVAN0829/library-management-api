server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/DUVAN0829/library-config
          default-label: main
          clone-on-start: true